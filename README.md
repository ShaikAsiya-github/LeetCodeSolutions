This is solution for two sum using HashMap where it stores data in key value pairs.. TC: O(n), SC: O(n)
Two pointer approach is also idle but doesnt work out for Unsorted array, if its sorted use Two pointer approach TC: O(n), SC: O(1)
