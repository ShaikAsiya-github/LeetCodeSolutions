package basic;
import java.util.*;

public class FindingDup {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//int arr[]= {1,2,2,3,4,5,5,6,7};
		int arr[]= {2,1,3,3};
		
		Set<Integer> hs= new HashSet<Integer>();
		
		for(int num: arr) {
			if(!hs.add(num)) {
//Is the trick used in Collection frameworks true: NOT already in set(adds nmbr) false: Already in set(set doesn't add to add duplicate ele which is false)
				System.out.println(num);
			}
		}

	}

}
